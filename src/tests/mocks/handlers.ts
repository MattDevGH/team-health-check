import { http, HttpResponse } from "msw";

// Add msw handlers here as you build out your API routes.
// See https://mswjs.io/docs/basics/request-handler

export const handlers = [
  // Example:
  // http.get("/api/items", () => HttpResponse.json([])),
];
